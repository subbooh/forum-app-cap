server:
    port: $PORT
---
spring:
  profiles: default
  datasource:
    url: "jdbc:sqlite:test.db?cache=shared"
    driver-class-name: org.sqlite.JDBC
    initialization-mode: always
    hikari:
      maximum-pool-size: 1
